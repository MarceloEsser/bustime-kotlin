<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        package="esser.marcelo.busoclock">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
            android:name=".MyApplication"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme"
            android:usesCleartextTraffic="true"
            tools:ignore="GoogleAppIndexingWarning"
            tools:targetApi="m">

        <activity android:name=".view.activity.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.activity.FavoriteSchedulesAcitivty" />
        <activity android:name=".view.activity.FavoriteLinesActivity" />
        <activity android:name=".view.activity.VicasaSchedulesActivity" />
        <activity android:name=".view.activity.SogalItinerariesActivity" />
        <activity android:name=".view.activity.SogalLinesActivity" />
        <activity android:name=".view.activity.SogalSchedulesActivity" />
        <activity android:name=".view.activity.VicasaLinesActivity" />

    </application>

</manifest>